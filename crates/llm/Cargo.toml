[package]
name = "familiar-llm"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "The LLM seam (periphery) — a boundary-gated consult. The model is not the factory."

[dependencies]
familiar-kernel.workspace = true
